/** Stud spacing on the x/z plane (world units). */
export const STUD = 8;
/** Height of a single plate on the y axis (world units). */
export const PLATE = 3.2;
/** Baseplate size in studs (square). */
export const BASE_STUDS = 32;
/** Half the baseplate; cells run from -HALF .. HALF-1. */
export const HALF = BASE_STUDS / 2;
